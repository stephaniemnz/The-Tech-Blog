<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Post</title>
    <link rel="stylesheet" href="/Public/public/css/style.css">
</head>
<body>
    <header>
        <h1>THE TECH BLOG</h1>
        <nav>
            <a href="/">Home</a>
            <a href="/login">Login</a>
        </nav>
    </header>
    <div class="container">
        <h2>{{title}}</h2>
        <p>{{body}}</p>
        <p class="post-date">Posted on {{createdAt}}</p>
        <h3>Comments</h3>
        {{#if comments}}
            <ul class="comment-list">
                {{#each comments}}
                    <li>
                        <p>{{comment}}</p>
                        <p class="comment-date">Posted on {{createdAt}}</p>
                        <p>By: {{username}}</p>
                    </li>
                {{/each}}
            </ul>
        {{else}}
            <p>No comments available.</p>
        {{/if}}
    </div>
    <footer>
        <p>&copy; 2024 THE TECH BLOG</p>
    </footer>
</body>
</html>
